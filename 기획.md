#  핵심 기능 

## 1. 가게 운영 및 레시피 관리 (Operations & Recipes)

젤라또는 배합 비율이 맛과 원가에 직결되므로 상세한 계산 기능이 핵심입니다.

|**기능 분류**|**상세 기능**|**비고**|
|---|---|---|
|**레시피 등록**|재료 구성(베이스, 맛 재료 등), 배합 비율(%), 제조 순서 기록||
|**원가 계산기**|입력된 재료 단가를 바탕으로 1통(Tub) 또는 1컵당 원가 자동 산출||
|**메뉴판 관리**|현재 매장에서 판매 중인 플레이버(시즌, 베스트 등) 상태 관리||
|**생산 기록**|특정 날짜에 생산한 플레이버와 수량 기록 (재고 차감과 연동)||

---

## 2. 재료 및 재고 관리 (Inventory)

재고 부족으로 인한 생산 중단을 방지하고 효율적인 발주를 돕는 기능입니다.

- **실시간 재고 현황:** 원재료(우유, 설탕 등)와 부자재(컵, 스푼, 포장지)의 현재 수량 파악
- **입/출고 관리:** 신규 재료 입고 처리 및 생산 시 자동 재고 차감 로직
- **재고 알림:** 설정한 '최소 재고량' 미만으로 떨어질 경우 하이라이트 또는 알림
- **구매 창구 연동:** 재료별 구매처 링크(쿠팡, 도매몰 등) 저장 및 바로가기 버튼
- **폐기 관리:** 유통기한 경과나 오염으로 인한 폐기 수량 및 사유 기록 |

---

## 3. 인력 관리 (Manpower/HR)

아르바이트생의 근무 스케줄과 실제 근무 시간을 투명하게 관리합니다.

- **직원 프로필:** 성명, 연락처, 시급, 보건증 갱신일 관리 |
- **근무 관리표(Scheduler):** 캘린더 형식의 주간/월간 근무 시간 배정
- **출퇴근 기록(T&A):** 직원이 매장 도착 시 버튼으로 출퇴근 시간을 기록하는 기능 (GPS 기반 권장)
- **체크리스트:** 오픈/미들/마감 시간대별 반드시 수행해야 할 업무 할당 및 완료 체크 
---

## 4. 급여 및 정산 관리 (Settlement)

근무 기록을 바탕으로 복잡한 수당 계산을 자동화합니다.

- **월별 급여 자동 계산:** `실제 근무 시간 x 시급` 계산
- **수당 계산기:** 주휴수당(주 15시간 이상 근무 시), 야간 수당 등 법정 수당 자동 합산
- **급여 명세서 생성:** 직원별 상세 내역(총 시간, 공제액, 최종 지급액) PDF 또는 이미지 생성
- **간이 손익 확인:** (선택 사항) 당일 매출액을 입력하면 인건비 및 재료비 비중 확인 


# 기술 스택 

## 🛠️ 추천 기술 스택 요약 (Tech Stack Roadmap)

| **구분**     | **추천 기술**                        | **선정 이유**                                |
| ---------- | -------------------------------- | ---------------------------------------- |
| **프론트엔드**  | **React + Vite + Tailwind CSS**  | 빠른 개발 속도와 Cursor AI의 높은 코드 생성 효율         |
| **상태 관리**  | **TanStack Query (React Query)** | 서버 데이터 동기화 및 캐싱 최적화에 필수                  |
| **백엔드/DB** | **Supabase (PostgreSQL)**        | 인증, 실시간 DB, 파일 저장소를 한 번에 해결 (SaaS 확장 용이) |
| **배포/호스팅** | **Vercel**                       | React 앱 배포 및 관리가 가장 간편하고 빠름              |
| **CI/CD**  | **GitHub Actions**               | 코드 푸시 시 자동 테스트 및 배포 자동화 구현 가능            |

---

## 1. 프론트엔드: React & Tailwind CSS

- **React (Vite):** 이미 사용 중인 프론트엔드 환경을 유지하되, 빌드 속도가 빠른 **Vite**를 추천합니다. Cursor AI가 가장 잘 이해하는 라이브러리이기도 합니다.
    
- **Tailwind CSS:** 디자인 시스템을 직접 구축할 필요 없이 클래스 기반으로 빠르게 UI를 짤 수 있습니다.
    
- **Shadcn UI:** Tailwind 기반의 완성형 컴포넌트 라이브러리로, 프로토타입임에도 불구하고 **상용 서비스 수준의 UI**를 즉시 만들어낼 수 있습니다.
    

## 2. 백엔드 및 데이터베이스: Supabase (강력 추천)

SaaS로 확장하려면 독자적인 서버를 구축하는 것보다 **BaaS(Backend as a Service)**인 **Supabase**가 훨씬 유리합니다.

- **PostgreSQL:** 젤라또 레시피와 재고, 직원 급여 등 복잡하게 얽힌 데이터를 관계형 데이터베이스로 안정적으로 관리할 수 있습니다.
- **Auth:** 남자친구뿐만 아니라 다른 사용자들의 회원가입 및 로그인을 쉽게 구현할 수 있습니다.
- **Row Level Security (RLS):** "사장님 A는 사장님 B의 매장 데이터를 볼 수 없다"는 핵심 보안 로직을 DB 단에서 쉽게 처리합니다.

## 3. 배포 및 인프라 (Deployment)

- **Vercel:** GitHub 저장소와 연결하면 `git push` 한 번으로 전 세계에 서비스가 배포됩니다.
- **Custom Domain:** 서비스화 단계에서 `my-gelato-admin.com` 같은 도메인을 붙여 전문성을 높일 수 있습니다.

## 4. CI/CD 및 품질 관리

- **GitHub Actions:** 코드를 원격 저장소에 올릴 때마다 자동으로 오류가 없는지 검사하고 Vercel에 새 버전을 배포합니다.
- **Zustand:** 가벼운 로컬 상태 관리가 필요할 때 (예: 레시피 입력 폼 임시 저장) 사용하기 좋습니다.

---

## 💡 SaaS화를 위한 핵심 설계 포인트

나중에 다른 매장 사장님들에게 서비스를 제공하고 싶다면, 설계 초기 단계에서 **'Tenant ID (매장 식별자)'** 개념을 도입해야 합니다.

- **데이터 분리:** 모든 테이블(`Recipes`, `Inventory`, `Staff`)에 `store_id` 컬럼을 추가하여, 각 사용자가 자신의 가게 데이터만 조회하도록 설계하세요.

- **MCP 활용:** Obsidian에 이 'Tenant ID 기반 데이터 구조'를 명시하고, Cursor에게 "@MCP 내용을 바탕으로 다중 사용자 로그인을 고려한 API 호출 로직을 짜줘"라고 요청하면 구현이 훨씬 쉬워집니다.

## 🚀 서버 및 호스팅 구성 계획

결론부터 말씀드리면, **백엔드 로직과 데이터베이스는 Supabase**에, **웹 애플리케이션 화면(프론트엔드)은 Vercel**에 올리게 됩니다.
### 1. 백엔드 및 데이터베이스 서버: Supabase
전통적인 'API 서버'와 'DB 서버'의 역할을 한 번에 해결합니다.
- **역할:** 젤라또 레시피, 재고 현황, 직원 출퇴근 기록 등 모든 데이터를 저장하고 관리합니다.
- **서버리스(Serverless):** 직접 서버를 띄울 필요 없이 Supabase가 제공하는 API를 통해 앱과 데이터를 주고받습니다.
- **인증 서버:** 사용자 로그인, 권한 관리(사장님/알바생 구분) 등 보안 로직도 이곳에서 처리됩니다.    
### 2. 프론트엔드 및 호스팅 서버: Vercel
사용자가 실제로 접속하는 '웹 사이트'를 호스팅합니다.
- **역할:** React로 만든 관리자 페이지 화면을 전 세계 사용자에게 빠르게 보여줍니다.
- **자동 배포:** GitHub에 코드를 올리면 Vercel이 이를 감지하여 자동으로 최신 버전을 서버에 반영(CI/CD)합니다.

---

## 💡 왜 이렇게 나누어 올리나요?

1. **관리의 편의성:** 서버의 보안 업데이트나 하드웨어 장애를 직접 신경 쓸 필요가 없습니다.
2. **비용 효율성:** 프로토타입 단계에서는 두 서비스 모두 **무료 플랜** 범위 내에서 충분히 운영 가능합니다.
3. **SaaS 확장성:** 나중에 사용자가 늘어나더라도 설정 하나로 서버 성능을 쉽게 확장할 수 있으며, 다중 사용자 환경(Tenant ID 기반 설계)을 지원하기에 매우 유리합니다.

---

## 🛠️ 다음 실행 단계

서버 환경이 결정되었으니, 이제 **Cursor**에서 실제 개발 환경을 세팅할 차례입니다.
1. **GitHub 저장소 생성:** 코드를 올릴 원격 저장소를 만듭니다.
2. **Supabase 프로젝트 생성:** 데이터를 담을 공간을 온라인상에 마련합니다.
3. **Vercel 연결:** GitHub 저장소와 Vercel을 연결하여 자동 배포 환경을 구축합니다.


---

# 📌 보강: 직원·알바 급여 모드 분리 (2026-05-13 추가)

위 3·4번 기능의 세부 동작을 staff 종류에 따라 분기합니다. **근무표·급여정산 UI는 통합**이지만 내부 로직은 고용형태에 따라 다릅니다.

## staff 종류 분류

| 구분 | `employment_types.pay_mode` | 급여 계산 | 근무 시간 |
|---|---|---|---|
| **직원·매니저** | `salary` | 기본급 + 상여금 (월 단위), 월 N회 휴무 적용 (기본 8회) | 매장 기본 시프트 시간 사용 |
| **아르바이트** | `hourly` | 시급 × 실근무시간 + 법정수당 (주휴·야간 등) | 개인별 시프트 시간 오버라이드 가능 |

## 인력 관리 보강 사항

- **직원 프로필** — 고용형태에 따라 입력 필드 분기:
  - salary 모드: 기본급, 월 휴무 의무일수 (employment_type 기본값)
  - hourly 모드: 시급, **개인별 오픈/미들/마감 시간 오버라이드** (매장 기본값 / 개별 설정 토글)
- **시프트 시간 해석 우선순위** — `getShiftSlotsForStaff(staffId, shift)` 헬퍼가 결정:
  1. `staff_shift_overrides`에 (staff_id, shift) 행이 있으면 → 그 시간 사용 (알바)
  2. 없으면 → `store_shift_settings` 매장 기본값 폴백 (직원)
- **근무표(주간·월간)** — 시프트 코드는 통합 표시, 내부 시각 해석만 헬퍼로 분기
- **출퇴근 baseline** — staff별 시프트 시간으로 자동 생성

## 급여 정산 보강 사항

- **통합 표, 행마다 컬럼 의미 다름**:
  - 직원 행: 기본급 표시 + 상여금 인라인 매월 입력 + 휴무 사용/의무 표시
  - 알바 행: `시급 × 실근무시간` + 법정수당 자동 합산
- **월 휴무 미달/초과**: 표시·경고만 (⚠ shortage / ℹ️ excess), 자동 감액 없음 — 사장이 수동 조정
- **상여금**: 매월 다름 → `payroll.bonus`에 매월 입력 (staff 마스터에 고정값 X)
- **산정 당시 모드 스냅샷**: `payroll.pay_mode` 컬럼에 저장 → 과거 정산 보존

## 신규 DB 객체 (Phase 9 마이그레이션)

- `employment_types`: `pay_mode`, `monthly_off_days` 컬럼 추가
- `staff`: `base_salary` 컬럼 추가 (salary 모드 전용)
- `staff_shift_overrides`: 신규 테이블 (개인별 시프트 시간)
- `payroll`: `pay_mode`, `bonus`, `off_days_used`, `off_days_required`, `off_days_warning` 컬럼 추가

> 상세 스키마·구현 순서는 [[설계-2026-05-13-v1.4]] 참조.
