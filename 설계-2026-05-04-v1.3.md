# 젤라또 가게 운영앱 설계 — v1.3

> 작성일: 2026-05-04  
> 이전 버전: [[설계-2026-04-29-v1.2]] · 데이터 흐름 상세: [[설계-인력-근무표-데이터흐름]]  
> 코드 기준 스냅샷: 실서비스 React 코드(`src/`) + Supabase SQL(`supabase/sql/`) — Phase 8(인력·근무표·출퇴근) 마무리 시점.

---

## 0. 이 문서가 다루는 것

v1.2 / 데이터흐름 문서에서 흩어져 있던 **DB 스키마**, **테이블별 컬럼 책임**, **프론트엔드 구조(훅·페이지·라이브러리)**, **데이터 파이프라인** 을 **현재 코드 기준으로** 한 곳에 통합 정리한 버전이다.

상위 두 문서는 **계획·맥락**, 본 문서는 **현 상태의 단일 레퍼런스**.

| 영역 | 단계 | 비고 |
|---|---|---|
| Phase 0~7 | ✅ 완료 | Auth · `ingredients` · `recipes` · `flavors` · `production_logs` |
| Phase 8 (Feature 3) | ✅ 1차 완료 | 직원 / 월간 / 주간 / 출퇴근 / 체크리스트 |
| Phase 8-후속 | 🟡 진행 중 | 출퇴근 수기 수정 OTP, 매장별 시프트 시각 DB 저장 |
| Phase 9 (Feature 4) | 🔒 착수 예정 | `payroll` 집계 |

---

## 1. 핵심 설계 원칙 (변경 없음)

1. **`store_id`** 는 모든 비-`stores` 테이블의 1열. SaaS 확장 대비.
2. **RLS** 는 모든 테이블에 처음부터. 패턴: `store_id IN (SELECT id FROM stores WHERE owner_id = auth.uid())`.
3. **예정(plan) ≠ 실적(actual)**: `schedule_*` 는 예정, `attendance` 는 실적. 급여는 실적 기준.
4. **재료의 단일 소스**: Feature 1 `ingredients` 가 Feature 2 재고관리로 자연 연결.
5. **원가 계산은 프론트 순수 함수** (`CostCalculator`).
6. **Supabase 쿼리는 항상 TanStack Query 훅으로 래핑**, 변경(mutation) 후에는 `invalidateQueries({ refetchType: 'active' })` 로 즉시 refetch 보장.

---

## 2. DB 스키마 — 테이블 카탈로그

### 2.1 Feature 1 — 레시피 / 메뉴

#### `stores`
매장(테넌트). owner = `auth.users`.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid PK | 매장 식별자. 모든 데이터의 분기점 |
| `owner_id` | uuid FK → `auth.users` | RLS의 1차 키 |
| `name` | text | 매장 이름 |
| `created_at` | timestamptz | 생성시각 |

#### `ingredients`
원재료 마스터. Feature 2 재고에서 수량만 추가될 예정.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid PK | |
| `store_id` | uuid FK | |
| `name` | text | 「우유」 「설탕」 같은 표시명 |
| `unit` | text | g, ml, ea 등 입력 단위 |
| `unit_price` | numeric | 단위당 매입가(원). 원가 계산의 입력값 |

#### `recipes` / `recipe_ingredients`
레시피 본체와 재료 구성(다대다 + 수량/단위/비율).

`recipe_ingredients`
- `amount`: 재료 사용량(레시피 단위 기준).
- `unit`: 「g」 「ml」 등.
- `ratio_pct`: 비율 표시용(선택).
- `recipe_id ON DELETE CASCADE` — 레시피 삭제 시 함께 정리.

#### `flavors`
판매하는 맛(메뉴). 레시피와 1:N (한 레시피로 여러 맛 가능 — `recipe_id` nullable).

| 컬럼 | 설명 |
|---|---|
| `recipe_id` | nullable. 레시피 미연결인 외부 도입 메뉴도 등록 가능 |
| `status` | `'active' \| 'season' \| 'soldout'` (CHECK) |
| `category` | 자유 텍스트 카테고리 |

#### `production_logs`
생산 기록. Feature 2 재고 차감의 원천이 될 예정.

---

### 2.2 Feature 2 — 재고 (스키마만, 미구현)

`inventory` (현재고) / `inventory_transactions` (입출고·폐기 이력 + `ref_production_log_id` 역참조).  
설계는 v1.2 그대로 유지.

---

### 2.3 Feature 3 — 인력 / 근무표 / 출퇴근

> SQL 원본: `supabase/sql/feature3_hr_tables.sql`,  
> `feature3_staff_default_shifts_unique_dow_shift.sql`,  
> `feature3_store_shift_settings.sql`.

#### `employment_types`
매장별 고용형태 마스터. (정규직/파트타임/단기 등).

| 컬럼 | 설명 |
|---|---|
| `code` | 매장 내 식별자 (`'fulltime'` 등) |
| `label` | 화면 표시명 |
| UNIQUE | `(store_id, code)` |

#### `staff`
직원 마스터.

| 컬럼 | 설명 |
|---|---|
| `employment_type_id` | nullable. 미지정 가능 |
| `name` / `phone` | 기본 정보 |
| `hourly_rate` | 시급(원). Phase 9 급여 계산 입력값 |
| `hire_date` | 입사일 |
| `health_cert_expires` | 보건증 만료일 |
| `photo_storage_path` / `resume_storage_path` / `health_cert_storage_path` | Supabase Storage 경로 |

#### `staff_default_shifts`
직원 프로필 — 요일별 기본 근무 패턴 (다중 시프트 허용).

| 컬럼 | 설명 |
|---|---|
| `day_of_week` | 0=일 ~ 6=토 |
| `shift` | `'open' \| 'middle' \| 'close'` |
| UNIQUE | `(staff_id, day_of_week, shift)` — v1.2에서 갱신: 같은 요일·같은 시프트 중복만 금지, **요일에 여러 시프트는 허용** |

#### `staff_calendar_assignments`
직원 미니달력. 특정 yyyymm + 일에 예외적인 시프트 지정.

| 컬럼 | 설명 |
|---|---|
| `yyyymm` | `'202605'` 형식 |
| `day` | 1 ~ 31 |
| UNIQUE | `(staff_id, yyyymm, day)` |

#### `schedule_month_cells`
**매장 전체 월간 근무표**. 날짜 × 직원 × 시프트.

| 컬럼 | 설명 |
|---|---|
| `work_date` | 날짜 |
| `shift` | 한 직원·한 날짜에 **단일** 시프트만 (UNIQUE `staff_id, work_date`) |

> 같은 요일에 직원 프로필이 여러 시프트를 가질 때, 「월별 기본 근무 세팅」은 `pickShiftForWeekday`(open → middle → close 우선순위) 규칙으로 **대표 한 개**만 채운다.

#### `schedule_week_slots` + `schedule_week_slot_assignees`
**주간 근무표 — 1시간 슬롯 × 요일** 그리드. 다대다 다중 배정.

`schedule_week_slots`
- `week_start` (월요일).
- `slot_index` 0=09:00, 1=10:00, …, 13=22:00.
- `day_index` 0=월 ~ 6=일.
- UNIQUE `(store_id, week_start, slot_index, day_index)`.

`schedule_week_slot_assignees`
- `slot_id ON DELETE CASCADE`.
- UNIQUE `(slot_id, staff_id)`.

#### `attendance`
**출퇴근 실적 + 기준선(baseline)** 한 테이블에 공존.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid PK | |
| `store_id` / `staff_id` | uuid FK | |
| `check_in` | timestamptz | UTC 저장 — 화면은 KST로 자동 변환 |
| `check_out` | timestamptz | 퇴근 전이면 NULL |
| `is_baseline` | boolean | **true** = 주간 근무표에서 자동 생성된 「예정」<br>**false** = 「실제 출퇴근」(Punch / 수기 수정) |
| `manually_corrected` | boolean | 수기 수정 여부 |
| `approved_by` | uuid FK → `staff` | 수기 수정 승인자 (※ FK 모호성 주의) |
| `approved_at` | timestamptz | 승인 시각 |
| `correction_note` | text | 수정 사유 메모 |

> **PostgREST 모호성 회피**: `staff` 를 임베드할 때 `staff_id` FK 와 `approved_by` FK 두 개가 있어 `select('staff(name)')` 표기는 모호 에러를 일으킨다. 항상 **`staff:staff_id ( name )`** 처럼 FK 명시.

> **`is_baseline` 분리 사용**: 「오늘 미종료 출근」 검색(`findOpenAttendanceToday`)은 반드시 `eq('is_baseline', false)` 필터로 실적 레코드만 본다. 그렇지 않으면 baseline의 `check_out` NULL 상태가 오검출된다.

#### `checklist_items` / `checklist_logs`
시간대별 체크리스트(매장 공통 항목 + 완료 로그).

#### `store_shift_settings` *(추가됨)*
매장별 오픈/미들/마감 슬롯 인덱스 설정.

| 컬럼 | 설명 |
|---|---|
| `store_id` PK | 매장 1행 |
| `open_anchor_slot_index` | 오픈 시프트 앵커 |
| `middle_anchor_slot_index` | 미들 앵커 |
| `close_anchor_slot_index` | 마감 앵커 |

> 이전엔 `dateUtils.ts` 상수(`SHIFT_ANCHOR_SLOT_INDEX`)였다. 매장별 운영시간 차이를 흡수하기 위해 분리.

---

### 2.4 Feature 4 — 급여 (스키마만)

#### `payroll`
| 컬럼 | 설명 |
|---|---|
| `period_start / period_end` | 정산 구간 |
| `base_pay` | 시급 × 실근무시간 합 |
| `allowances` | 수당(주휴·야간 등 — 수식은 미결) |
| `total` | base_pay + allowances |

> 산출 입력: `attendance` 의 `is_baseline = false` 행만. baseline 은 **참조용**이지 입력값이 아니다.

---

## 3. RLS 정책 패턴

```sql
ALTER TABLE <table> ENABLE ROW LEVEL SECURITY;

CREATE POLICY "store owner only" ON <table>
USING (
  store_id IN (
    SELECT id FROM stores WHERE owner_id = auth.uid()
  )
);
```

`stores` 자체는 `owner_id = auth.uid()`. 모든 자식 테이블은 위 패턴 동일.  
INSERT 시에는 별도 `WITH CHECK` 로 동일 조건 — `phase2_stores_insert_policy.sql` 참조.

---

## 4. 프론트엔드 구조 (현 코드)

```
src/
├── lib/
│   ├── supabase.ts            ← 클라이언트
│   ├── dateUtils.ts           ← 슬롯/요일/시프트 라벨, 시각 변환
│   ├── plannedFromWeek.ts     ← 주간 슬롯 → 예정 상세(start/endHm, bandLabel)
│   ├── attendanceDisplay.ts   ← attendance rows → 날짜·직원 그룹화·diff
│   ├── ensureStore.ts         ← 로그인 직후 stores 1행 보장
│   ├── authErrors.ts / postgresErrors.ts
│   └── utils.ts
│
├── hooks/                     ← 모두 TanStack Query 래핑
│   ├── useStore.ts
│   ├── useStaff.ts
│   ├── useEmploymentTypes.ts
│   ├── useStaffWeekPlan.ts
│   ├── useStaffCalendar.ts
│   ├── useMonthSchedule.ts    ← schedule_month_cells, applyMonthDefault…, applyWeekFromMonth
│   ├── useWeekSchedule.ts     ← schedule_week_slots(+assignees) CRUD
│   ├── useShiftSettings.ts    ← store_shift_settings
│   ├── useAttendance.ts       ← list / punchIn / punchOut / correct / baseline
│   ├── useChecklist.ts
│   ├── useRecipes.ts / useIngredients.ts
│   └── (Feature 1 기타)
│
├── stores/
│   └── recipeFormStore.ts     ← Zustand: 레시피 폼 임시 저장
│
├── components/
│   ├── ui/                    ← Shadcn UI (button, card, input, label)
│   ├── auth/                  ← ProtectedRoute, GuestRoute, AuthLoading
│   ├── layout/AppLayout.tsx
│   └── recipes/CostCalculator.tsx
│       (참고) WeekDragGrid / MonthCalGrid 는 StaffPage 내부 섹션으로 통합되어 있음
│
├── contexts/auth-context.tsx  ← Supabase Auth 세션
│
└── pages/
    ├── HomePage.tsx
    ├── LoginPage.tsx / SignupPage.tsx
    ├── RecipesPage.tsx / RecipeFormPage.tsx / RecipeDetailPage.tsx
    ├── IngredientsPage.tsx
    └── StaffPage.tsx          ← 직원 프로필 / 근무표(월·주) / 출퇴근 / 체크리스트
```

페이지가 모놀리식인 곳(특히 `StaffPage.tsx`)은 향후 Phase 9 들어가기 전 **섹션별 컴포넌트 분리** 가 권장된다.

---

## 5. UI ↔ DB 매핑 (Feature 3 위주, 현 상태)

| UI 위치 | 동작 | 사용 훅 / 함수 | 영향 받는 테이블 |
|---|---|---|---|
| 직원 탭 — 프로필 추가/수정 | 직원 등록·수정 | `useStaff` | `staff`, `employment_types` |
| 직원 상세 — 근무요일 행 | 요일×시프트 패턴 입력 | `useStaffWeekPlan` | `staff_default_shifts` |
| 직원 상세 — 미니달력 | 월/일 단위 예외 시프트 | `useStaffCalendar` | `staff_calendar_assignments` |
| 근무표 탭 — 월간 그리드 | 셀 클릭 → 직원·시프트 배정 | `useMonthSchedule` | `schedule_month_cells` |
| 근무표 탭 — «월별 기본 근무 세팅» | 프로필 기반 일괄 채움 | `useApplyMonthDefaultFromProfiles` | `schedule_month_cells` (+ `staff_calendar_assignments` 동기화) |
| 근무표 탭 — 주간 그리드 | 슬롯 드래그/클릭 배정 | `useWeekSchedule` | `schedule_week_slots`, `schedule_week_slot_assignees` |
| 근무표 탭 — «이번 주 ← 월간표 반영» | 월간 셀 → 주간 슬롯 일괄 | `useApplyWeekFromMonthCells` | `schedule_week_slot_assignees` |
| 근무표 탭 — 주간 chip × | 단건 배정 삭제 | `useRemoveWeekSlotAssignee` | `schedule_week_slot_assignees` |
| 출퇴근 탭 — 자동 기준 생성 | 주간 저장 시 baseline INSERT | `useGenerateAttendanceBaseline` | `attendance (is_baseline=true)` |
| 출퇴근 탭 — «출근» 버튼 | 실적 INSERT | `usePunchIn` | `attendance (is_baseline=false)` |
| 출퇴근 탭 — «퇴근» 버튼 | 미종료 실적 UPDATE | `usePunchOut` | `attendance` |
| 출퇴근 탭 — «수정» 버튼 | check_in/out 수기 수정 | `useCorrectAttendance` | `attendance.manually_corrected = true` |
| 출퇴근 탭 — 상태 라벨 | 지각/근무시간 초과/조기출근/정상 | `diffPlannedVsActualCheckIn` + endHm 계산 | (DB 쓰기 없음, 표시 전용) |
| 체크리스트 탭 — 항목 추가/완료 | | `useChecklist` 계열 | `checklist_items`, `checklist_logs` |

---

## 6. 데이터 파이프라인 (현 구현)

```
[직원 프로필]
  staff_default_shifts (요일별 다중 시프트 허용)
       │
       │  «월별 기본 근무 세팅»
       │  pickShiftForWeekday(open→middle→close)
       ▼
[월간 매장 뷰]
  schedule_month_cells  ── 동기화 ──▶  staff_calendar_assignments
       │
       │  «이번 주 ← 월간표 반영» (앵커 슬롯)
       ▼
[주간 1h 슬롯 그리드]
  schedule_week_slots + schedule_week_slot_assignees
       │
       │  «주간 저장» 시 자동
       │  generateAttendanceBaselineForWeek
       ▼
[출퇴근 baseline]
  attendance (is_baseline = true)
       │
       │  «출근/퇴근» 버튼 / 수기 수정
       ▼
[출퇴근 실적]
  attendance (is_baseline = false)
       │
       ▼
[Phase 9] payroll
  base_pay = Σ(실근무시간) × hourly_rate
  (baseline 은 참조·비교용이지 산정 입력 아님)
```

---

## 7. 시간대 / 시각 처리 규칙

- **DB 저장**: `timestamptz` (UTC).
- **앱 INSERT**: `new Date().toISOString()` (UTC).
- **화면 표시**: `Date.toLocaleString()` 또는 `fmtTimeShort` 가 자동으로 브라우저 로컬(KST) 변환.
- **날짜 필터**: `from`/`to` 의 YYYY-MM-DD 는 **로컬 자정 → ISO(UTC)** 로 변환 후 비교 (`ymdToLocalIsoStart/End`). `'YYYY-MM-DDT00:00:00'` 같은 timezone 미지정 문자열은 UTC 자정으로 해석되어 KST 09시 이전 데이터가 누락되니 **금지**.
- **로컬 일자 키**: `localDateKeyFromCheckIn(iso) = formatYmdLocal(new Date(iso))` — DB UTC를 KST 로 변환한 뒤 YMD 추출.

---

## 8. 출퇴근 표시 컴포넌트 규약 (`PersonBlock`)

| 표시 영역 | 소스 | 비고 |
|---|---|---|
| **예정** 라벨 | `plannedMap` (= `plannedDetailForStaffDay`) | `schedule_week_slots` 에서 도출. 매일 첫·마지막 슬롯으로 `startHm`, `endHm` 산출 |
| **실제** 시각 | `displayRecord = actual ?? baseline` | 실적이 있으면 실적 우선, 없으면 baseline 폴백. baseline 폴백 표시에는 `(기준)` 뱃지 |
| 출근 / 퇴근 버튼 | 실적 레코드 기준 | `hasActualPunchIn / hasActualPunchOut` (baseline 무시). 같은 라인에 **수정** 버튼 함께 |
| 상태 라벨 | `statusLabel` | `late > over > early > ok` 우선순위. 5분 임계. 출근 전엔 미표시 |

### 상태 라벨 정의

| 라벨 | 조건 |
|---|---|
| 지각 (late) | 실제 `check_in` − 예정 `startHm` > 5분 |
| 조기출근 (early) | 예정 `startHm` − 실제 `check_in` > 5분 |
| 근무시간 초과 (over) | 실제 `check_out` − 예정 `endHm` > 5분 |
| 정상 (ok) | 출퇴근 모두 있고 위 3개 모두 아님 |

---

## 9. 데이터 정합성 규칙

| 규칙 | 적용 위치 |
|---|---|
| `attendance` 미종료 검사는 **실적만** | `findOpenAttendanceToday` 의 `.eq('is_baseline', false)` |
| `attendance` SELECT 의 staff 임베드는 FK 명시 | `staff:staff_id ( name )` |
| mutation 후 invalidate 는 **await + active refetch** | `await invalidateQueries({queryKey:['attendance'], refetchType:'active'})` |
| 같은 날·같은 직원 baseline 중복 생성 금지 | `generateAttendanceBaselineForWeek` 가 동일 날 첫 행 발견 시 skip |
| `useAttendanceList` 캐시 정책 | `staleTime: 0`, `refetchOnMount: 'always'`, `refetchOnWindowFocus: true` |
| `schedule_month_cells` 1직원·1날짜 = 1시프트 | DB UNIQUE 제약 |
| 직원 프로필 1요일 다중 시프트 허용 | UNIQUE `(staff_id, dow, shift)` 로 변경 (마이그레이션 적용됨) |

---

## 10. 미결 사항 (의사결정 필요)

1. **`schedule_month_cells` ↔ `staff_calendar_assignments` 단일 소스 통합 여부** — 현재는 양쪽에 동기화 INSERT.
2. **출퇴근 수기 수정 OTP 채널** — 카카오워크 Webhook vs Twilio SMS. 현재 인증 없이 수정 가능(임시).
3. **주휴/야간 수당 수식** — 근로기준법 시나리오(주 단위 15h 집계 등) 정교화. Phase 9에서 결정.
4. **급여 명세서 출력** — PDF / 이미지 생성 방식.
5. **`StaffPage.tsx` 섹션 분리** — Phase 9 이전에 컴포넌트 분리 권장(현재 1,800줄 규모).

---

## 11. 변경 이력 (이 문서 기준)

| 날짜 | 변경 |
|---|---|
| 2026-04-29 | v1.2 — `schedule_week_slot_assignees`, `attendance` 수기수정·is_baseline 컬럼 추가 |
| 2026-04-30 | 인력·근무표 데이터흐름 문서 분리 작성 |
| 2026-05-04 | **본 v1.3 통합본**. `store_shift_settings` 추가, `staff_default_shifts` UNIQUE 변경, `attendance` 모호성/RLS/캐시 규칙 명문화, 프론트엔드 구조 코드 기준 갱신, 출퇴근 UI 상태 라벨/(기준) 폴백/수정 버튼 위치 정의 |

---

## 12. 다음 작업 (체크리스트 연계)

- [ ] Phase 8-후속: `store_shift_settings` UI 노출 + 매장별 오픈/미들/마감 시각 편집.
- [ ] Phase 8-후속: 출퇴근 수기 수정 OTP 채널 결정 후 미들웨어 연결.
- [ ] Phase 9 착수: `payroll` 테이블 + RLS 마이그레이션, 월별 정산 화면.
- [ ] `StaffPage.tsx` 섹션 분리 리팩터링 (`StaffProfileSection`, `StaffScheduleSection`, `StaffAttendanceSection`, `StaffChecklistSection`).
