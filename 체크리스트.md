# 개발 체크리스트

> 설계 기준: [[설계-2026-04-29-v1.2]]
> Feature 1 (레시피 & 운영) 프로토타입 → Feature 2~4 순차 확장

---

## Phase 0 — 개발 환경 세팅

- [x] GitHub 저장소 생성 (`gelato-os`)
- [x] Vite + React + TypeScript 프로젝트 초기화 (Vite 8 + React 19 + TS 6)
- [x] Tailwind CSS v4 설정 (`@tailwindcss/vite` 플러그인)
- [x] Shadcn UI 설치 및 초기 설정
- [x] TanStack Query + Zustand 설치
- [x] `src/lib/supabase.ts` + `.env.example` 작성
- [x] Supabase 프로젝트 생성 (대시보드에서 직접)
- [x] `.env` 파일에 Supabase URL / anon key 추가
- [x] Vercel 연결 (GitHub 저장소 → 자동 배포)

---

## Phase 1 — DB 세팅 (Feature 1 테이블)

- [x] `stores` 테이블 생성 + RLS 적용
- [x] `ingredients` 테이블 생성 + RLS 적용
- [x] `recipes` 테이블 생성 + RLS 적용
- [x] `recipe_ingredients` 테이블 생성 + RLS 적용
- [x] `flavors` 테이블 생성 + RLS 적용
- [x] `production_logs` 테이블 생성 + RLS 적용
- [x] Supabase SQL Editor에서 전체 마이그레이션 실행 확인

> 💡 SQL은 `설계-2026-04-27.md`의 Feature 1 스키마 그대로 복붙

---

## Phase 2 — 인증 (Auth)

- [x] Supabase Auth 이메일/비밀번호 로그인 활성화
- [x] 로그인 / 회원가입 페이지 구현
- [x] 로그인 후 `stores` 테이블에 매장 자동 생성 로직 구현
- [x] 비로그인 시 리다이렉트 처리

---

## Phase 3 — 재료 관리 (`ingredients`)

- [x] `useIngredients.ts` 훅 작성 (TanStack Query)
- [x] 재료 목록 페이지
- [x] 재료 추가 / 수정 / 삭제
- [x] 재료 단가 수정 시 레시피 쿼리 무효화로 원가 표시 갱신 (레시피 상세 재조회)

---

## Phase 4 — 레시피 관리 (`recipes` + `recipe_ingredients`)

- [x] `useRecipes.ts` 훅 작성
- [x] 레시피 목록 페이지
- [x] 레시피 등록 폼 (재료 선택 + 배합 비율 입력)
  - [x] Zustand `recipeFormStore` 로 입력 중간 저장 (신규 등록만)
- [x] 레시피 상세 페이지
- [x] `CostCalculator.tsx` 구현
  - [x] 1통 원가 계산: `Σ (unit_price × amount)`
  - [x] 1컵 원가 계산: `1통 원가 / 통당 컵 수`
  - [x] 원가율 계산: `1컵 원가 / 판매가 × 100 (%)`
- [x] 레시피 상세에서 원가 실시간 표시 확인

---

## Phase 5 — 메뉴판 관리 (`flavors`)

- [ ] `useFlavors.ts` 훅 작성
- [ ] 메뉴판 페이지 (플레이버 목록)
- [ ] 플레이버 추가 (레시피 연결)
- [ ] 상태 변경: `active` / `season` / `soldout` 토글
- [ ] 카테고리 필터 (베스트 / 시즌 / 기본)

---

## Phase 6 — 생산 기록 (`production_logs`)

- [ ] `useProduction.ts` 훅 작성
- [ ] 생산 기록 입력 폼 (플레이버 선택 + 생산 통 수 + 날짜)
- [ ] 날짜별 생산 기록 조회 페이지
- [ ] *(Feature 2 연동 준비)* 생산 기록 저장 시 `inventory_transactions` INSERT 자리 비워두기

> ✅ 여기까지 완료되면 **Feature 1 프로토타입 완성**

---

## Phase 7 — Feature 2: 재고 관리 🔒

> Feature 1 완성 후 진행

- [ ] `inventory` 테이블 생성 + RLS
- [ ] `inventory_transactions` 테이블 생성 + RLS
- [ ] 생산 기록 저장 시 재고 자동 차감 로직 연결
- [ ] 실시간 재고 현황 페이지
- [ ] 입고 처리 기능
- [ ] 최소 재고 알림 (하이라이트)
- [ ] 폐기 기록

---

## Phase 8 — Feature 3: 인력 관리 🔒

> DB: `supabase/sql/feature3_hr_tables.sql` 실행 후 앱 연동  
> UI 참고: `gelato-management-prototype-v1.5.html` · 설계 `설계-2026-04-29-v1.2.md` · **근무표 데이터 흐름** `설계-인력-근무표-데이터흐름.md`

- [x] `employment_types`, `staff`, `staff_default_shifts`, `staff_calendar_assignments`, `schedule_week_slots`, `schedule_week_slot_assignees`, `schedule_month_cells`, `attendance`, `checklist_items`, `checklist_logs` 테이블 + RLS (마이그레이션 파일)
- [x] 고용형태 + 직원 프로필 CRUD (`useEmploymentTypes`, `useStaff`)
- [x] 직원 상세: 근무요일·형태 (`staff_default_shifts`), **근무 달력**은 주간 근무표 저장 데이터 연동(읽기 전용·「근무」표시)
- [x] 근무표 탭: **주간**(드래그 배정·저장·확인 대화상자)·**근무관리**(시프트 시간) — **월간 근무표 UI는 숨김**(스키마·훅은 보존, 추후 복구 가능) (`useWeekSchedule`, `useMonthSchedule`, `useShiftSettings`)
- [x] 출퇴근 탭: **저장된 주간 슬롯 → 예정 시간**(프로토타입 v1.5 레이아웃)·실적 기록·기준선 생성 (`useAttendance`, `mergePlannedByStaffDate`, `fetchWeekSlots`) — 급여(Phase 9) 연동 준비
- [x] 체크리스트 탭: 구역별 항목 · 완료 로그 (`useChecklist`)
- [ ] *(후속)* 출퇴근 수기 수정 OTP · 스토리지 이력서/사진 업로드

---

## Phase 8-1 — 인력 근무표·출퇴근 연동 (설계 반영)

> 데이터 흐름 단일 문서: [[설계-인력-근무표-데이터흐름]] · 상위 [[설계-2026-04-29-v1.2]]  
> 구현은 Phase 8 스키마·탭을 전제로 한 **후속 연동·UI 정합** 단계다.

- [x] 설계 문서 `설계-인력-근무표-데이터흐름.md` 기준 (우선순위는 구현에 반영)
- [x] ~~월간 `schedule_month_cells` → 주간 덮어쓰기~~ → **직원 프로필 근무요일·시프트(`staff_default_shifts`)** + 근무관리 시간대 → **`useApplyWeekFromProfileDefaults`** 로 해당 주 슬롯 일괄 적용(버튼: **«근무표 가져오기»**)
- [x] **근무관리** 서브탭: `stores.shift_*` 오픈·미들·마감 시작/종료 (`useShiftTimeSettings`, SQL `feature3_store_shift_settings`)
- [x] 주간: 로컬 초안 **`usePersistWeekDraft`** 확정 저장, **저장 시** 출퇴근 예정·기준 연동 안내 `confirm` (v1.5와 같이 **같은 요일 열 안 세로 드래그**로 연속 배정)
- [x] **월간 근무표 UI** 탭·패널 **비노출**(코드·`useMonthSchedule` 등 보존; 추후 복구 시 문서·체크리스트 갱신)
- [x] 기준 출퇴근 생성: 주간 **첫/마지막 슬롯** 시각·v1.5 (`generateAttendanceBaselineForWeek`)
- [x] 출퇴근 탭: 날짜·직원 그룹, **예정(주간 근무표 병합)** vs 실제, 지각·조기출근 표시 (`plannedFromWeek`, 프로토타입 v1.5 스타일 카드)
- [x] 직원 프로필 근무 달력: **`useStaffWeekCalendarPlans`** / 월이 걸치는 주간 슬롯 조회 — 편집 없음, 호버·모달로 상세 시각

---

## Phase 9 — Feature 4: 급여 & 정산 🔒

> `staff.hourly_rate` + `attendance` 실근무 집계와 연동 (Phase 8 완료 후 본격 구현)

- [ ] `payroll` 테이블 생성
- [ ] 월별 급여 자동 계산 (실근무시간 × 시급)
- [ ] 주휴수당 / 야간수당 자동 합산
- [ ] 급여 명세서 생성 (PDF or 이미지)
- [ ] 간이 손익 확인 (매출 입력 → 인건비/재료비 비중)
