# 개발 체크리스트

> 설계 기준: [[설계-2026-04-29-v1.2]]
> Feature 1 (레시피 & 운영) 프로토타입 → Feature 2~4 순차 확장

---

## Phase 0 — 개발 환경 세팅

- [x] GitHub 저장소 생성 (`gelato-os`)
- [x] Vite + React + TypeScript 프로젝트 초기화 (Vite 8 + React 19 + TS 6)
- [x] Tailwind CSS v4 설정 (`@tailwindcss/vite` 플러그인)
- [x] Shadcn UI 설치 및 초기 설정
- [x] TanStack Query + Zustand 설치
- [x] `src/lib/supabase.ts` + `.env.example` 작성
- [ ] Supabase 프로젝트 생성 (대시보드에서 직접)
- [ ] `.env` 파일에 Supabase URL / anon key 추가
- [ ] Vercel 연결 (GitHub 저장소 → 자동 배포)

---

## Phase 1 — DB 세팅 (Feature 1 테이블)

- [ ] `stores` 테이블 생성 + RLS 적용
- [ ] `ingredients` 테이블 생성 + RLS 적용
- [ ] `recipes` 테이블 생성 + RLS 적용
- [ ] `recipe_ingredients` 테이블 생성 + RLS 적용
- [ ] `flavors` 테이블 생성 + RLS 적용
- [ ] `production_logs` 테이블 생성 + RLS 적용
- [ ] Supabase SQL Editor에서 전체 마이그레이션 실행 확인

> 💡 SQL은 `설계-2026-04-27.md`의 Feature 1 스키마 그대로 복붙

---

## Phase 2 — 인증 (Auth)

- [ ] Supabase Auth 이메일/비밀번호 로그인 활성화
- [ ] 로그인 / 회원가입 페이지 구현
- [ ] 로그인 후 `stores` 테이블에 매장 자동 생성 로직 구현
- [ ] 비로그인 시 리다이렉트 처리

---

## Phase 3 — 재료 관리 (`ingredients`)

- [ ] `useIngredients.ts` 훅 작성 (TanStack Query)
- [ ] 재료 목록 페이지
- [ ] 재료 추가 / 수정 / 삭제
- [ ] 재료 단가 수정 시 전체 레시피 원가 자동 재계산 확인

---

## Phase 4 — 레시피 관리 (`recipes` + `recipe_ingredients`)

- [ ] `useRecipes.ts` 훅 작성
- [ ] 레시피 목록 페이지
- [ ] 레시피 등록 폼 (재료 선택 + 배합 비율 입력)
  - [ ] Zustand `recipeFormStore` 로 입력 중간 저장
- [ ] 레시피 상세 페이지
- [ ] `CostCalculator.tsx` 구현
  - [ ] 1통 원가 계산: `Σ (unit_price × amount)`
  - [ ] 1컵 원가 계산: `1통 원가 / 통당 컵 수`
  - [ ] 원가율 계산: `1컵 원가 / 판매가 × 100 (%)`
- [ ] 레시피 상세에서 원가 실시간 표시 확인

---

## Phase 5 — 메뉴판 관리 (`flavors`)

- [ ] `useFlavors.ts` 훅 작성
- [ ] 메뉴판 페이지 (플레이버 목록)
- [ ] 플레이버 추가 (레시피 연결)
- [ ] 상태 변경: `active` / `season` / `soldout` 토글
- [ ] 카테고리 필터 (베스트 / 시즌 / 기본)

---

## Phase 6 — 생산 기록 (`production_logs`)

- [ ] `useProduction.ts` 훅 작성
- [ ] 생산 기록 입력 폼 (플레이버 선택 + 생산 통 수 + 날짜)
- [ ] 날짜별 생산 기록 조회 페이지
- [ ] *(Feature 2 연동 준비)* 생산 기록 저장 시 `inventory_transactions` INSERT 자리 비워두기

> ✅ 여기까지 완료되면 **Feature 1 프로토타입 완성**

---

## Phase 7 — Feature 2: 재고 관리 🔒

> Feature 1 완성 후 진행

- [ ] `inventory` 테이블 생성 + RLS
- [ ] `inventory_transactions` 테이블 생성 + RLS
- [ ] 생산 기록 저장 시 재고 자동 차감 로직 연결
- [ ] 실시간 재고 현황 페이지
- [ ] 입고 처리 기능
- [ ] 최소 재고 알림 (하이라이트)
- [ ] 폐기 기록

---

## Phase 8 — Feature 3: 인력 관리 🔒

> Feature 2 완성 후 진행

- [ ] `staff`, `schedules`, `attendance`, `checklist_items`, `checklist_logs` 테이블 생성
- [ ] 직원 프로필 CRUD
- [ ] 주간/월간 스케줄러
- [ ] 출퇴근 기록 (버튼 탭)
- [ ] 오픈/미들/마감 체크리스트

---

## Phase 9 — Feature 4: 급여 & 정산 🔒

> Feature 3 완성 후 진행

- [ ] `payroll` 테이블 생성
- [ ] 월별 급여 자동 계산 (실근무시간 × 시급)
- [ ] 주휴수당 / 야간수당 자동 합산
- [ ] 급여 명세서 생성 (PDF or 이미지)
- [ ] 간이 손익 확인 (매출 입력 → 인건비/재료비 비중)
