# 젤라또 가게 운영앱 아키텍처 설계

> 작성일: 2026-04-27
> 기반 문서: [[기획]]


---

## 이 문서의 설계 범위

### 핵심기능 1 — 가게 운영 및 레시피 관리 ✅ (프로토타입 구현 대상)

기획서의 4가지 핵심 기능 중 **Feature 1을 프로토타입으로 먼저 구현**한다.
Feature 2~4는 이 문서에서 DB 스키마까지만 설계해두고, 실제 구현은 Feature 1 완성 후 순차적으로 진행한다.

| 기능 | 설계 포함 여부 | 구현 여부 |
|---|---|---|
| **Feature 1** 레시피 등록 / 원가 계산 / 메뉴판 관리 / 생산 기록 | ✅ DB 스키마 + 프론트 구조 + 데이터 흐름 | ✅ 프로토타입 구현 |
| **Feature 2** 재료 & 재고 관리 | ✅ DB 스키마 설계 (Feature 1 테이블 재사용 구조) | 🔒 Feature 1 완성 후 |
| **Feature 3** 인력 관리 | ✅ DB 스키마 설계 (테이블 목록) | 🟡 HTML 프로토타입 v1.1에서 UI·데이터 모델 검증 중 |
| **Feature 4** 급여 & 정산 | ✅ DB 스키마 설계 (테이블 목록) | 🔒 Feature 3 완성 후 |

### Feature 1 세부 항목

기획서 기준 Feature 1의 4가지 상세 기능을 모두 이 설계에 포함한다.

| 상세 기능 | 설계 내용 |
|---|---|
| 레시피 등록 (재료 구성, 배합 비율, 제조 순서) | `recipes` + `recipe_ingredients` 테이블 |
| 원가 계산기 (1통 / 1컵당 원가 자동 산출) | 프론트 순수 함수 계산 (`CostCalculator`) |
| 메뉴판 관리 (플레이버 상태 관리) | `flavors` 테이블 (status enum) |
| 생산 기록 (날짜별 수량, 재고 차감 연동 준비) | `production_logs` 테이블 |

---

## 핵심 설계 원칙

1. **`store_id`를 모든 테이블에 처음부터 추가** — 나중에 SaaS로 확장할 때 DB 구조를 갈아엎는 일을 막기 위해 프로토타입 단계부터 적용
2. **Feature 1의 `ingredients` 테이블이 Feature 2 재고관리로 자연 연결** — 재료 정보를 한 곳에서 관리하고 재고 수량만 추가하는 구조
3. **`production_logs`가 Feature 1-2 사이의 다리 역할** — 생산 기록이 나중에 재고 자동 차감 트리거로 연결됨
4. **RLS(Row Level Security)를 처음부터 적용** — "사장 A는 사장 B 매장 데이터 못 봄" 보안 로직을 DB 단에서 처리

---

## DB 스키마 (Supabase / PostgreSQL)

### Feature 1 구현 테이블 (프로토타입에서 실제 생성)

```sql
-- 매장 정보
CREATE TABLE stores (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  owner_id   uuid NOT NULL REFERENCES auth.users(id),
  name       text NOT NULL,
  created_at timestamptz DEFAULT now()
);

-- 재료 정보 (Feature 2 재고관리와 공유)
CREATE TABLE ingredients (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  name       text NOT NULL,        -- "우유", "설탕", "피스타치오 페이스트"
  unit       text NOT NULL,        -- "ml", "g", "개"
  unit_price numeric NOT NULL      -- 재료 단가 (원)
);

-- 레시피
CREATE TABLE recipes (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id    uuid NOT NULL REFERENCES stores(id),
  name        text NOT NULL,       -- "피스타치오 젤라또"
  tub_size_g  numeric,             -- 목표 1통 용량(g)
  memo        text,                -- 제조 순서 등 메모
  created_at  timestamptz DEFAULT now()
);

-- 레시피-재료 배합표 (N:M 조인)
CREATE TABLE recipe_ingredients (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  recipe_id     uuid NOT NULL REFERENCES recipes(id) ON DELETE CASCADE,
  ingredient_id uuid NOT NULL REFERENCES ingredients(id),
  amount        numeric NOT NULL,  -- 사용량
  unit          text NOT NULL,     -- "g", "ml"
  ratio_pct     numeric           -- 배합 비율(%)
);

-- 메뉴판 (플레이버 상태 관리)
CREATE TABLE flavors (
  id        uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id  uuid NOT NULL REFERENCES stores(id),
  recipe_id uuid REFERENCES recipes(id),
  name      text NOT NULL,
  status    text NOT NULL CHECK (status IN ('active', 'season', 'soldout')),
  category  text                   -- "베스트", "시즌", "기본"
);

-- 생산 기록 (Feature 2 재고 차감과 연동 준비)
CREATE TABLE production_logs (
  id             uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id       uuid NOT NULL REFERENCES stores(id),
  flavor_id      uuid NOT NULL REFERENCES flavors(id),
  quantity_tubs  numeric NOT NULL, -- 생산 통 수
  produced_at    timestamptz NOT NULL DEFAULT now()
);
```

### Feature 2~4 확장 시 추가될 테이블 (지금은 스키마만 설계)

```sql
-- Feature 2: 재고 관리
-- ingredients 테이블 재사용, 재고 수량만 별도 관리
CREATE TABLE inventory (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id      uuid NOT NULL REFERENCES stores(id),
  ingredient_id uuid NOT NULL REFERENCES ingredients(id),
  quantity      numeric NOT NULL DEFAULT 0,   -- 현재 재고량
  min_threshold numeric                        -- 최소 재고 알림 기준
);

CREATE TABLE inventory_transactions (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id      uuid NOT NULL REFERENCES stores(id),
  ingredient_id uuid NOT NULL REFERENCES ingredients(id),
  type          text NOT NULL CHECK (type IN ('in', 'out', 'discard')),
  quantity      numeric NOT NULL,
  reason        text,                          -- 폐기 사유 등
  ref_production_log_id uuid REFERENCES production_logs(id), -- 생산 기록 연동
  created_at    timestamptz DEFAULT now()
);

-- Feature 3: 인력 관리 (2026-04-29 프로토타입 반영)
-- 고용형태: 매니저/직원/아르바이트 + 매장별 커스텀 코드 확장
CREATE TABLE employment_types (
  id       uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL REFERENCES stores(id),
  code     text NOT NULL,
  label    text NOT NULL,
  UNIQUE (store_id, code)
);

CREATE TABLE staff (
  id                         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id                   uuid NOT NULL REFERENCES stores(id),
  employment_type_id         uuid REFERENCES employment_types(id),
  name                       text NOT NULL,
  phone                      text,
  hourly_rate                numeric NOT NULL,
  hire_date                  date,
  health_cert_expires        date,
  photo_storage_path         text,
  resume_storage_path        text,
  health_cert_storage_path   text
);

-- 기본 근무요일·형태 (요일당 1행, 오픈/미들/마감)
CREATE TABLE staff_default_shifts (
  id           uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id     uuid NOT NULL REFERENCES stores(id),
  staff_id     uuid NOT NULL REFERENCES staff(id),
  day_of_week  smallint NOT NULL CHECK (day_of_week BETWEEN 0 AND 6),
  shift        text NOT NULL CHECK (shift IN ('open', 'middle', 'close')),
  UNIQUE (staff_id, day_of_week)
);

-- 직원별 월 달력 셀 (일 단위 근무 형태)
CREATE TABLE staff_calendar_assignments (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  staff_id   uuid NOT NULL REFERENCES staff(id),
  yyyymm     text NOT NULL,
  day        smallint NOT NULL CHECK (day BETWEEN 1 AND 31),
  shift      text NOT NULL CHECK (shift IN ('open', 'middle', 'close')),
  UNIQUE (staff_id, yyyymm, day)
);

-- 주간 근무표: 시간대(세로) × 요일(가로) 슬롯에 직원 배정
CREATE TABLE schedule_week_slots (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  week_start date NOT NULL,
  slot_index smallint NOT NULL,
  day_index  smallint NOT NULL CHECK (day_index BETWEEN 0 AND 6),
  staff_id   uuid REFERENCES staff(id),
  UNIQUE (store_id, week_start, slot_index, day_index)
);

-- 월간 근무표: 직원 × 일자별 오픈/미들/마감
CREATE TABLE schedule_month_cells (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  staff_id   uuid NOT NULL REFERENCES staff(id),
  work_date  date NOT NULL,
  shift      text NOT NULL CHECK (shift IN ('open', 'middle', 'close')),
  UNIQUE (staff_id, work_date)
);

-- (선택) 시각 범위가 명확히 필요할 때만 사용 — 주간 슬롯·월간 셀과 병행 가능
CREATE TABLE schedules (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  staff_id   uuid NOT NULL REFERENCES staff(id),
  date       date NOT NULL,
  start_time time NOT NULL,
  end_time   time NOT NULL
);

CREATE TABLE attendance (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  staff_id   uuid NOT NULL REFERENCES staff(id),
  check_in   timestamptz,
  check_out  timestamptz
);

CREATE TABLE checklist_items (
  id       uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL REFERENCES stores(id),
  shift    text NOT NULL CHECK (shift IN ('open', 'middle', 'close')),
  title    text NOT NULL
);

CREATE TABLE checklist_logs (
  id              uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id        uuid NOT NULL REFERENCES stores(id),
  checklist_item_id uuid NOT NULL REFERENCES checklist_items(id),
  staff_id        uuid REFERENCES staff(id),
  completed_at    timestamptz DEFAULT now()
);

-- Feature 4: 급여 & 정산
CREATE TABLE payroll (
  id           uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id     uuid NOT NULL REFERENCES stores(id),
  staff_id     uuid NOT NULL REFERENCES staff(id),
  period_start date NOT NULL,
  period_end   date NOT NULL,
  base_pay     numeric NOT NULL,   -- 기본급 (실근무시간 × 시급)
  allowances   numeric DEFAULT 0,  -- 주휴수당 + 야간수당
  total        numeric NOT NULL,   -- 최종 지급액
  created_at   timestamptz DEFAULT now()
);
```

**프로토타입(`gelato-management-prototype-v1.1.html`)과의 대응**

| UI / 메모리 구조 | DB 초안 |
|---|---|
| 직원 상세의 고용형태 + «형태 등록» | `employment_types` + `staff.employment_type_id` |
| 근무요일·형태 행 (`workRules`) | `staff_default_shifts` |
| 달력 셀(월 이동·일별 오피/미들/마감 순환) | `staff_calendar_assignments` (`yyyymm` + `day`) |
| 주간 표: 시간대 × 요일 셀에 직원 선택 | `schedule_week_slots` |
| 월간 표: 직원 × 일자 셀렉트 | `schedule_month_cells` |
| 사진·이력서·보건증 파일 입력 | `photo_storage_path` 등 Storage 경로 (실구현 시 Supabase Storage) |

---

## RLS 정책 (모든 테이블에 동일한 패턴 적용)

```sql
-- stores 테이블 예시 (나머지 테이블도 동일한 패턴)
ALTER TABLE stores ENABLE ROW LEVEL SECURITY;

CREATE POLICY "store owner only"
ON stores
USING (owner_id = auth.uid());

-- store_id를 가진 테이블 예시 (recipes, ingredients 등)
ALTER TABLE recipes ENABLE ROW LEVEL SECURITY;

CREATE POLICY "store owner only"
ON recipes
USING (
  store_id IN (
    SELECT id FROM stores WHERE owner_id = auth.uid()
  )
);
```

---

## 프론트엔드 구조

```
src/
├── lib/
│   └── supabase.ts              ← Supabase 클라이언트 (싱글톤)
│
├── hooks/                       ← TanStack Query 훅 (서버 데이터 전담)
│   ├── useRecipes.ts            -- 레시피 CRUD
│   ├── useIngredients.ts        -- 재료 CRUD
│   ├── useFlavors.ts            -- 메뉴판 상태 관리
│   └── useProduction.ts         -- 생산 기록
│
├── stores/                      ← Zustand (폼 임시 상태만)
│   └── recipeFormStore.ts       -- 레시피 입력 중간 저장
│
├── components/
│   ├── ui/                      ← Shadcn UI 기본 컴포넌트
│   └── CostCalculator.tsx       ← 원가 계산 (순수 함수, DB 조회 없음)
│
└── pages/
    ├── recipes/                 ← Feature 1 ✅
    │   ├── RecipeListPage.tsx
    │   ├── RecipeDetailPage.tsx
    │   └── RecipeFormPage.tsx
    ├── flavors/                 ← Feature 1 ✅
    │   └── FlavorBoardPage.tsx
    ├── production/              ← Feature 1 ✅
    │   └── ProductionLogPage.tsx
    ├── inventory/               ← Feature 2 🔒
    ├── staff/                   ← Feature 3 🔒
    └── payroll/                 ← Feature 4 🔒
```

---

## 원가 계산 로직

DB 조회 없이 프론트에서 순수 계산 (TanStack Query로 캐싱된 데이터 사용):

```
1통 원가 = Σ (재료별 unit_price × amount)
1컵 원가 = 1통 원가 / 통당 컵 수 (사용자 입력)
원가율   = 1컵 원가 / 판매가 × 100 (%)
```

`recipe_ingredients`와 `ingredients.unit_price`를 조인하면 계산에 필요한 모든 값이 나옴.
단가가 바뀌면 TanStack Query 캐시 무효화 → 모든 레시피 원가 자동 재계산.

---

## 데이터 흐름 (Feature 1)

```
[재료 등록]
  └→ ingredients INSERT

[레시피 등록]
  └→ recipes INSERT
  └→ recipe_ingredients INSERT (재료별 배합 비율)
  └→ CostCalculator (프론트 계산) → 1통/1컵 원가 실시간 표시

[메뉴판 등록]
  └→ flavors INSERT (recipe_id 연결, status 설정)

[생산 기록]
  └→ production_logs INSERT
  └→ (Feature 2 연동 시) inventory_transactions INSERT → inventory UPDATE 트리거
```

---

## 개발 순서

| 단계 | 작업 | 결과물 |
|---|---|---|
| **1** | GitHub 저장소 생성 + Supabase 프로젝트 생성 | 개발 환경 |
| **2** | Feature 1 테이블 스키마 마이그레이션 + RLS 적용 | DB 완성 |
| **3** | Supabase Auth 연동 (로그인 / 매장 생성) | 인증 |
| **4** | `ingredients` CRUD | 재료 등록 화면 |
| **5** | `recipes` + `recipe_ingredients` CRUD + 원가 계산 | 레시피 + 원가 자동 계산 |
| **6** | `flavors` 상태 관리 | 메뉴판 화면 |
| **7** | `production_logs` 기록 | 생산 기록 화면 |
| **8** | Feature 2: `inventory` 테이블 추가 + 재고 차감 연동 | 재고 관리 |

---

## 기술 스택 요약

| 구분 | 기술 | 이유 |
|---|---|---|
| 프론트엔드 | React + Vite + Tailwind CSS + Shadcn UI | 빠른 개발, Cursor AI 친화적 |
| 서버 상태 | TanStack Query | 캐싱 + 자동 재조회 |
| 로컬 상태 | Zustand | 폼 임시 저장 등 가벼운 상태 |
| 백엔드/DB | Supabase (PostgreSQL + Auth + RLS) | 인증·DB·보안 한 번에 해결, SaaS 확장 용이 |
| 배포 | Vercel + GitHub Actions | git push → 자동 배포 |
