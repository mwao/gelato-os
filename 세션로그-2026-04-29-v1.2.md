# 세션 로그 — 2026-04-29 (가게운영앱 프로토타입 v1.2)

## 이번 세션에서 한 일

- **신규 파일** `gelato-management-prototype-v1.2.html` 작성
  - v1.1(인력·급여 데모) 기반으로 수정 사항 4건 반영.

---

### 1. 주간 근무표 — 드래그 다중선택 + 1시간 간격

- `WEEK_SLOT_LABELS`를 2시간 단위 → **1시간 단위** 14개 슬롯(09:00~22:00)으로 변경.
- `WEEK_GRID` 값을 단일 `staffId` 문자열 → **배열 `string[]`** 로 변경해 셀당 다수 근무자 지원.
- 셀렉박스 UI 제거 → **근무자 선택 바 + 드래그/클릭 페인팅** 방식으로 교체.
  - 상단 bar에서 근무자(또는 지우기) 선택 → 셀 클릭·드래그로 배정.
  - 같은 요일 내 세로 드래그로 연속 시간대 일괄 배정 가능.
  - 터치(모바일) 지원: `touchstart` / `touchmove` / `touchend` 핸들러 추가.
  - 배정된 근무자는 셀 안에 컬러 chip으로 표시.
- **DB 대응**: `schedule_week_slots` 스키마에서 `staff_id` 단일 FK → `JSONB` 배열 또는 별도 `schedule_week_slot_assignees` 조인 테이블 추가 필요 (설계 문서 반영).

---

### 2. 월간 근무표 — 달력 UI + 근무자 뱃지 + 모달 편집

- 기존 직원×일자 셀렉박스 행렬 테이블 → **7열 달력 레이아웃**으로 전면 교체.
- 각 날짜 셀에 근무자 이름 chip 표시 (오픈=초록, 미들=파랑, 마감=주황).
- **마우스오버 팝업**: 근무자 chip 위에 마우스를 올리면 예정 근무형태 + 실제 출퇴근 시간 툴팁 표시.
- **+ 버튼 / chip 클릭** → 해당 날짜 근무 배정 모달 열기 (직원별 오픈·미들·마감·미근무 선택).
- **직원 프로필 달력 연동**: 모달에서 저장 시 `staff.calendar[ym][day]`와 `MONTH_SHIFT` 양방향 동기화. 직원 상세 달력이 열려 있으면 즉시 갱신.
- **DB 대응**: `schedule_month_cells` ↔ `staff_calendar_assignments` 둘 중 하나로 통합하거나 뷰로 합산하는 방향 권장 (설계 문서 반영).

---

### 3. 출퇴근 수기 수정 + 관리자 인증 흐름

- 이전 기록(ATT_HISTORY) 각 행에 **「수정」** 버튼 추가.
- 오늘 출퇴근 행에도 **「수기 입력」·「수정」** 버튼 추가.
- 수정 모달: 날짜·출근시간·퇴근시간 편집 필드.
- **관리자 인증 흐름**:
  1. 「🔐 관리자 인증 요청」 → alert으로 발송 안내 (프로토타입 코드: `1234`).
  2. 인증번호 입력·확인 후에만 저장 활성화.
  3. 인증 성공 시 확인 버튼 텍스트 `✓ 인증 완료`로 변경.
- 실서비스 구현 방향: 별도 기획서 항목으로 분리 (하단 다음 세션 참고).
- **DB 대응**: `attendance` 테이블에 `manually_corrected boolean`, `approved_by uuid`, `approved_at timestamptz` 컬럼 추가 필요 (설계 문서 반영).

---

### 4. 예정 스케줄 vs 실제 근무 — 아이디어 제안 (미구현, 검토용)

아래는 클로드가 제안하는 설계 방향입니다. 채택 여부는 다음 세션에서 결정 후 구현합니다.

**핵심 문제**: 주간·월간 근무표(예정)와 실제 출퇴근 기록(실적)이 별도 데이터이므로, 당일 상황(조기 퇴근, 대타, 긴급 교대)을 어떻게 반영할지 설계가 필요.

**제안 A — 당일 실적 자동 생성 방식** (권장)
- 근무표는 `schedule_*` 테이블에 "예정" 그대로 유지.
- 출퇴근 체크인 시 `attendance` 레코드 생성 → 이것이 "실제 근무".
- 급여 모듈은 `attendance` 기반으로 집계, 예정표는 참고용으로만 사용.
- 차이 감지: `schedule_month_cells`와 `attendance` JOIN 후 불일치 행을 UI에서 ⚠️ 아이콘으로 표시.

**제안 B — 당일 실적 탭 분리 방식**
- 근무표 탭에 "예정" / "실적" 서브탭 추가.
- 실적 탭은 당일 출퇴근 후 관리자가 최종 확인(approve)하는 workflow.
- 대타 발생 시 실적 탭에서 담당자를 변경, 예정표는 건드리지 않음.

**제안 C — 스케줄 기반 출퇴근 유도 방식**
- 출근 체크 시 해당 직원의 당일 예정 시프트를 자동 조회해 화면에 표시.
- "오픈 9:00~15:00 예정" 안내 후 실제 체크인 시간과 비교해 지각·조퇴 여부 자동 판별.
- 카카오워크/슬랙 Webhook으로 관리자에게 즉시 알림.

**추천**: A + C 조합. 예정은 예정, 실적은 `attendance`. 체크인 시 예정 시프트 표시로 UX 개선.

---

## 발생한 문제 및 해결

- `WEEK_GRID` 구조 변경(단일 → 배열)으로 기존 샘플 초기화 로직 수정.
- `seedMonthShiftDemo` 함수의 달력 → `MONTH_SHIFT` 연동 유지.
- 팝업 함수 내 전각 괄호 문자 오타 수정.
- `initStaffPayrollUI` 함수 구조 분리 오류 수정.

## 다음 세션에서 할 일 (제안)

- [ ] 예정/실적 구분 설계 방향 결정 후 UI 반영 (A·B·C 중 선택).
- [ ] 출퇴근 버튼 실서비스 인증: 카카오워크 Webhook 또는 Twilio SMS OTP 연동.
- [ ] 주간 근무표 `schedule_week_slot_assignees` 스키마 확정 및 Supabase 마이그레이션.
- [ ] 월간·주간 근무표 데이터 통합 여부 결정 (`schedule_month_cells` vs `staff_calendar_assignments`).
- [ ] 명세서 PDF/이미지 보내기 및 Supabase 스키마 마이그레이션.
- [ ] 주휴·야간 수식 실제 근로기준법 시나리오에 맞게 분리.

## 현재 프로젝트 상태

- **v1.0**: 레시피·메뉴판·원가·생산 프로토타입.
- **v1.1**: 인력·급여·연동 데모.
- **v1.2**: 주간근무표 드래그 배정, 월간근무표 달력 UI, 출퇴근 수기수정·관리자인증 흐름 추가. **기획·설계 검증용** 단계.
- 실서비스 전 HTTPS 호스팅 후 PWA 설치·manifest 검증 권장.
