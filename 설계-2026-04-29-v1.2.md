# 젤라또 가게 운영앱 아키텍처 설계

> 작성일: 2026-04-27  
> 최종 갱신: 2026-04-29 (v1.2 반영)  
> 기반 문서: [[기획]]

---

## 이 문서의 설계 범위

### 핵심기능 1 — 가게 운영 및 레시피 관리 ✅ (프로토타입 구현 대상)

| 기능 | 설계 포함 여부 | 구현 여부 |
|---|---|---|
| **Feature 1** 레시피 등록 / 원가 계산 / 메뉴판 관리 / 생산 기록 | ✅ DB 스키마 + 프론트 구조 + 데이터 흐름 | ✅ 프로토타입 구현 |
| **Feature 2** 재료 & 재고 관리 | ✅ DB 스키마 설계 | 🔒 Feature 1 완성 후 |
| **Feature 3** 인력 관리 | ✅ DB 스키마 설계 | 🟡 HTML 프로토타입 v1.2에서 UI·데이터 모델 검증 중 |
| **Feature 4** 급여 & 정산 | ✅ DB 스키마 설계 | 🟡 HTML 프로토타입 v1.2 기본 연동 |

### Feature 1 세부 항목

| 상세 기능 | 설계 내용 |
|---|---|
| 레시피 등록 (재료 구성, 배합 비율, 제조 순서) | `recipes` + `recipe_ingredients` 테이블 |
| 원가 계산기 (1통 / 1컵당 원가 자동 산출) | 프론트 순수 함수 계산 (`CostCalculator`) |
| 메뉴판 관리 (플레이버 상태 관리) | `flavors` 테이블 (status enum) |
| 생산 기록 (날짜별 수량, 재고 차감 연동 준비) | `production_logs` 테이블 |

---

## 핵심 설계 원칙

1. **`store_id`를 모든 테이블에 처음부터 추가** — 나중에 SaaS로 확장할 때 DB 구조를 갈아엎는 일을 막기 위해 프로토타입 단계부터 적용
2. **Feature 1의 `ingredients` 테이블이 Feature 2 재고관리로 자연 연결** — 재료 정보를 한 곳에서 관리하고 재고 수량만 추가하는 구조
3. **`production_logs`가 Feature 1-2 사이의 다리 역할** — 생산 기록이 나중에 재고 자동 차감 트리거로 연결됨
4. **RLS(Row Level Security)를 처음부터 적용** — "사장 A는 사장 B 매장 데이터 못 봄" 보안 로직을 DB 단에서 처리
5. **예정 스케줄 ≠ 실제 근무** — `schedule_*` 테이블은 "예정", `attendance` 테이블은 "실적"으로 명확히 분리. 급여는 `attendance` 기준으로 산출.

---

## DB 스키마 (Supabase / PostgreSQL)

### Feature 1 구현 테이블 (프로토타입에서 실제 생성)

```sql
CREATE TABLE stores (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  owner_id   uuid NOT NULL REFERENCES auth.users(id),
  name       text NOT NULL,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE ingredients (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  name       text NOT NULL,
  unit       text NOT NULL,
  unit_price numeric NOT NULL
);

CREATE TABLE recipes (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id    uuid NOT NULL REFERENCES stores(id),
  name        text NOT NULL,
  tub_size_g  numeric,
  memo        text,
  created_at  timestamptz DEFAULT now()
);

CREATE TABLE recipe_ingredients (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  recipe_id     uuid NOT NULL REFERENCES recipes(id) ON DELETE CASCADE,
  ingredient_id uuid NOT NULL REFERENCES ingredients(id),
  amount        numeric NOT NULL,
  unit          text NOT NULL,
  ratio_pct     numeric
);

CREATE TABLE flavors (
  id        uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id  uuid NOT NULL REFERENCES stores(id),
  recipe_id uuid REFERENCES recipes(id),
  name      text NOT NULL,
  status    text NOT NULL CHECK (status IN ('active', 'season', 'soldout')),
  category  text
);

CREATE TABLE production_logs (
  id             uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id       uuid NOT NULL REFERENCES stores(id),
  flavor_id      uuid NOT NULL REFERENCES flavors(id),
  quantity_tubs  numeric NOT NULL,
  produced_at    timestamptz NOT NULL DEFAULT now()
);
```

### Feature 2~4 확장 시 추가될 테이블

```sql
-- Feature 2: 재고 관리
CREATE TABLE inventory (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id      uuid NOT NULL REFERENCES stores(id),
  ingredient_id uuid NOT NULL REFERENCES ingredients(id),
  quantity      numeric NOT NULL DEFAULT 0,
  min_threshold numeric
);

CREATE TABLE inventory_transactions (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id      uuid NOT NULL REFERENCES stores(id),
  ingredient_id uuid NOT NULL REFERENCES ingredients(id),
  type          text NOT NULL CHECK (type IN ('in', 'out', 'discard')),
  quantity      numeric NOT NULL,
  reason        text,
  ref_production_log_id uuid REFERENCES production_logs(id),
  created_at    timestamptz DEFAULT now()
);

-- Feature 3: 인력 관리
CREATE TABLE employment_types (
  id       uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL REFERENCES stores(id),
  code     text NOT NULL,
  label    text NOT NULL,
  UNIQUE (store_id, code)
);

CREATE TABLE staff (
  id                         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id                   uuid NOT NULL REFERENCES stores(id),
  employment_type_id         uuid REFERENCES employment_types(id),
  name                       text NOT NULL,
  phone                      text,
  hourly_rate                numeric NOT NULL,
  hire_date                  date,
  health_cert_expires        date,
  photo_storage_path         text,
  resume_storage_path        text,
  health_cert_storage_path   text
);

-- 기본 근무요일·형태 (요일당 1행)
CREATE TABLE staff_default_shifts (
  id           uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id     uuid NOT NULL REFERENCES stores(id),
  staff_id     uuid NOT NULL REFERENCES staff(id),
  day_of_week  smallint NOT NULL CHECK (day_of_week BETWEEN 0 AND 6),
  shift        text NOT NULL CHECK (shift IN ('open', 'middle', 'close')),
  UNIQUE (staff_id, day_of_week)
);

-- 직원별 월 달력 셀 (일 단위, 예정 근무 형태)
-- ※ schedule_month_cells와 통합 검토 중 (v1.2 미결)
CREATE TABLE staff_calendar_assignments (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  staff_id   uuid NOT NULL REFERENCES staff(id),
  yyyymm     text NOT NULL,
  day        smallint NOT NULL CHECK (day BETWEEN 1 AND 31),
  shift      text NOT NULL CHECK (shift IN ('open', 'middle', 'close')),
  UNIQUE (staff_id, yyyymm, day)
);

-- 주간 근무표: 시간대(세로) × 요일(가로) 슬롯에 직원 다수 배정
-- v1.2 변경: 단일 staff_id FK → 다대다 조인 테이블로 분리
CREATE TABLE schedule_week_slots (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  week_start date NOT NULL,
  slot_index smallint NOT NULL,  -- 0=09:00, 1=10:00, ..., 13=22:00 (1시간 단위)
  day_index  smallint NOT NULL CHECK (day_index BETWEEN 0 AND 6),
  UNIQUE (store_id, week_start, slot_index, day_index)
);

-- v1.2 추가: 주간 슬롯 ↔ 직원 다대다 (다중 배정 지원)
CREATE TABLE schedule_week_slot_assignees (
  id       uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  slot_id  uuid NOT NULL REFERENCES schedule_week_slots(id) ON DELETE CASCADE,
  staff_id uuid NOT NULL REFERENCES staff(id),
  UNIQUE (slot_id, staff_id)
);

-- 월간 근무표: 날짜별 근무자 배정 (예정)
-- ※ 달력 UI 기반으로 staff_calendar_assignments와 통합 여부 검토 중
CREATE TABLE schedule_month_cells (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  staff_id   uuid NOT NULL REFERENCES staff(id),
  work_date  date NOT NULL,
  shift      text NOT NULL CHECK (shift IN ('open', 'middle', 'close')),
  UNIQUE (staff_id, work_date)
);

-- (선택) 시각 범위가 명확히 필요할 때 사용
CREATE TABLE schedules (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id   uuid NOT NULL REFERENCES stores(id),
  staff_id   uuid NOT NULL REFERENCES staff(id),
  date       date NOT NULL,
  start_time time NOT NULL,
  end_time   time NOT NULL
);

-- 출퇴근 실적 (예정 스케줄과 분리된 "실제 근무")
-- v1.2 추가: 수기 수정 추적 컬럼
CREATE TABLE attendance (
  id                   uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id             uuid NOT NULL REFERENCES stores(id),
  staff_id             uuid NOT NULL REFERENCES staff(id),
  check_in             timestamptz,
  check_out            timestamptz,
  -- 수기 수정 추적 (v1.2)
  manually_corrected   boolean DEFAULT false,
  approved_by          uuid REFERENCES staff(id),  -- 승인한 관리자
  approved_at          timestamptz,
  correction_note      text
);

CREATE TABLE checklist_items (
  id       uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL REFERENCES stores(id),
  shift    text NOT NULL CHECK (shift IN ('open', 'middle', 'close')),
  title    text NOT NULL
);

CREATE TABLE checklist_logs (
  id                uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id          uuid NOT NULL REFERENCES stores(id),
  checklist_item_id uuid NOT NULL REFERENCES checklist_items(id),
  staff_id          uuid REFERENCES staff(id),
  completed_at      timestamptz DEFAULT now()
);

-- Feature 4: 급여 & 정산
CREATE TABLE payroll (
  id           uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id     uuid NOT NULL REFERENCES stores(id),
  staff_id     uuid NOT NULL REFERENCES staff(id),
  period_start date NOT NULL,
  period_end   date NOT NULL,
  base_pay     numeric NOT NULL,
  allowances   numeric DEFAULT 0,
  total        numeric NOT NULL,
  created_at   timestamptz DEFAULT now()
);
```

---

## 예정 스케줄 vs 실제 근무 설계 방향 (v1.2 기획 항목 — 미결)

프로토타입에서 제안된 3가지 방향. 다음 세션에서 채택 방향 결정 후 구현.

| 방안 | 개요 | 장점 | 단점 |
|---|---|---|---|
| **A (권장)** | 예정=`schedule_*`, 실적=`attendance`. 급여는 실적 기준. | 단순, 책임 명확 | 차이 감지 UI 추가 필요 |
| **B** | 근무표에 예정/실적 서브탭 + 관리자 최종 확인(approve) workflow | 감사 추적 좋음 | 관리 overhead |
| **C** | 체크인 시 예정 시프트 안내 + 지각·조퇴 자동 판별 | UX 좋음 | 예정과 실적 항상 비교 필요 |

추천: **A + C 조합** — 예정/실적 테이블 분리 유지, 체크인 화면에서 당일 예정 시프트 표시.

---

## 출퇴근 수기 수정 인증 흐름 (v1.2 기획 — 실서비스 구현 예정)

```
직원이 수정 요청
  → 서버에서 6자리 OTP 생성 → 관리자 연락처로 발송
     (카카오워크 Webhook / Twilio SMS 중 선택)
  → 관리자가 OTP 전달
  → 직원이 OTP 입력 → 서버 검증 (5분 만료)
  → attendance.manually_corrected=true, approved_by=관리자 ID 기록
```

프로토타입: alert + 하드코드 `1234` 로 흐름 시뮬레이션.

---

**프로토타입(`gelato-management-prototype-v1.2.html`)과의 대응**

| UI / 메모리 구조 | DB 초안 |
|---|---|
| 직원 상세 고용형태 + 형태 등록 | `employment_types` + `staff.employment_type_id` |
| 근무요일·형태 행 (`workRules`) | `staff_default_shifts` |
| 달력 셀(월별 오픈/미들/마감) | `staff_calendar_assignments` |
| 주간 표: 1h 슬롯 × 요일, 다중 배정 | `schedule_week_slots` + `schedule_week_slot_assignees` |
| 월간 표: 달력 셀 + 모달 배정 | `schedule_month_cells` (or `staff_calendar_assignments` 통합 검토) |
| 월간 근무표 ↔ 직원 프로필 달력 연동 | 양방향 동기화, 실서비스 단일 소스는 `schedule_month_cells` 권장 |
| 출퇴근 수기 수정 + 관리자 OTP | `attendance.manually_corrected / approved_by / approved_at` |
| 사진·이력서·보건증 파일 | Storage 경로 컬럼 (Supabase Storage) |

---

## RLS 정책 (모든 테이블에 동일한 패턴 적용)

```sql
ALTER TABLE stores ENABLE ROW LEVEL SECURITY;
CREATE POLICY "store owner only" ON stores USING (owner_id = auth.uid());

ALTER TABLE recipes ENABLE ROW LEVEL SECURITY;
CREATE POLICY "store owner only" ON recipes
USING (store_id IN (SELECT id FROM stores WHERE owner_id = auth.uid()));
-- 나머지 테이블도 동일한 패턴
```

---

## 프론트엔드 구조

```
src/
├── lib/
│   └── supabase.ts
├── hooks/
│   ├── useRecipes.ts
│   ├── useIngredients.ts
│   ├── useFlavors.ts
│   ├── useProduction.ts
│   ├── useStaff.ts          ← Feature 3
│   ├── useSchedule.ts       ← Feature 3
│   └── useAttendance.ts     ← Feature 3 (실적 전담)
├── stores/
│   └── recipeFormStore.ts
├── components/
│   ├── ui/
│   ├── CostCalculator.tsx
│   ├── WeekDragGrid.tsx     ← v1.2: 드래그 다중선택 주간 근무표
│   └── MonthCalGrid.tsx     ← v1.2: 달력형 월간 근무표
└── pages/
    ├── recipes/
    ├── flavors/
    ├── production/
    ├── inventory/           ← Feature 2 🔒
    ├── staff/               ← Feature 3 🟡
    └── payroll/             ← Feature 4 🟡
```

---

## 원가 계산 로직

```
1통 원가 = Σ (재료별 unit_price × amount)
1컵 원가 = 1통 원가 / 통당 컵 수
원가율   = 1컵 원가 / 판매가 × 100 (%)
```

---

## 데이터 흐름 (Feature 3·4 — v1.2 기준)

```
[근무 예정 입력]
  └→ schedule_week_slots + schedule_week_slot_assignees INSERT  (주간)
  └→ schedule_month_cells INSERT  (월간)
  └→ staff_calendar_assignments INSERT  (직원 상세 달력)
  ※ 월간 표와 직원 달력은 동기화 — 실서비스에서 단일 소스로 통합 권장

[출퇴근 실적]
  └→ attendance INSERT (check_in/check_out)
  └→ 수기 수정 시: manually_corrected=true + OTP 승인 흐름

[급여 산출]
  └→ attendance JOIN staff → 실근무시간 집계
  └→ payroll INSERT (base_pay + allowances + total)
  ※ 예정 스케줄이 아닌 실적(attendance) 기준
```

---

## 개발 순서

| 단계 | 작업 | 결과물 |
|---|---|---|
| **1** | GitHub 저장소 생성 + Supabase 프로젝트 생성 | 개발 환경 |
| **2** | Feature 1 테이블 스키마 마이그레이션 + RLS | DB |
| **3** | Supabase Auth 연동 | 인증 |
| **4** | `ingredients` CRUD | 재료 등록 |
| **5** | `recipes` + `recipe_ingredients` + 원가 계산 | 레시피 |
| **6** | `flavors` 상태 관리 | 메뉴판 |
| **7** | `production_logs` | 생산 기록 |
| **8** | Feature 2: `inventory` + 재고 차감 | 재고 관리 |
| **9** | Feature 3: `staff` + `schedule_*` + `attendance` | 인력·근무표 |
| **10** | Feature 3 OTP 인증 (Kakao/Twilio) | 수기수정 승인 |
| **11** | Feature 4: `payroll` 집계 + 명세서 PDF | 급여 정산 |

---

## 기술 스택 요약

| 구분 | 기술 | 이유 |
|---|---|---|
| 프론트엔드 | React + Vite + Tailwind CSS + Shadcn UI | 빠른 개발 |
| 서버 상태 | TanStack Query | 캐싱 + 자동 재조회 |
| 로컬 상태 | Zustand | 폼 임시 저장 |
| 백엔드/DB | Supabase (PostgreSQL + Auth + RLS) | 인증·DB·보안 통합 |
| 알림/OTP | 카카오워크 Webhook 또는 Twilio SMS | 관리자 인증 |
| 배포 | Vercel + GitHub Actions | 자동 배포 |
